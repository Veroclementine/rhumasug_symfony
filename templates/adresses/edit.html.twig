{% extends 'base.html.twig' %}

{% block title %}Edit Adresses{% endblock %}

{% block body %}
{{ include("partials/title_section.html.twig", {'name':'Edit Address'}) }}
    <div class="login_register_wrap section">
        <div class="container">
            <div class="row justify-content-center">
                <div class="col-xl-6 col-md-10">
                    <div class="login_wrap">
                        <div class="padding_eight_all bg-white">
                            <div class="heading_s1">
                                <h3>Edit Adresses</h3>
                            </div>
                            <div class="content">
                              {{ include('adresses/_form.html.twig', {'button_label': 'Mettre à jour '}) }}
                             <a class="btn btn-border-fill btn-block mt-2" href="{{ path('account') }}">Retour au compte</a>

                            </div>
                            
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

  
{% endblock %}
